import Maps from '../components/googleMaps';

const detail = () => {
    return (
        <div>
            {/* Price, Room details, Address, Image, Booking URL, Amenities (if we can) */}
            THIS IS A DETAIL PAGE!
            <Maps />
        </div>
    );
}

export default detail;